# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*tfstate*
*.tfplan
.terraform.tfstate.lock.info
*.tfstate.lock.info
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# SSH Keys (never commit private keys)
terra-key*
terra-key*.pub
*.pem

# OS
.DS_Store